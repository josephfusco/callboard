vendor/
node_modules/
build/
artifacts/
test-results/
playwright-report/
.phpcs.cache
.wp-env.override.json
plugin-check.json
*.zip
.DS_Store
tests/fixtures/callboard/splash/

# WordPress writes its intermediate sizes next to the fixture covers when the dev sites import them
tests/fixtures/callboard/*/cover-*.png

# scratch output must never land in the package
undefined/
*.png
!assets/*.png
!site/*.png
!tests/fixtures/**/*.png
tests/fixtures/callboard/*/cover-*.png

# Written by .github/scripts/detect-public-surface.js when run locally.
comment.md

# A set built from your own audio, for local listening only. Never committed.
tests/fixtures/callboard/mine/
tests/fixtures/callboard/local-*/
